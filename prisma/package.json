{
  "name": "@uzeed/prisma",
  "private": true,
  "prisma": {
    "seed": "node seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "argon2": "^0.31.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  },
  "scripts": {
    "generate": "prisma generate --schema=schema.prisma",
    "migrate:deploy": "prisma migrate deploy --schema=schema.prisma",
    "db:seed": "prisma db seed --schema=schema.prisma",
    "seed:test": "node scripts/seed-test-profiles.js",
    "clean:test": "node scripts/clean-test-profiles.js",
    "seed:umate": "node scripts/seed-umate-creators.js",
    "clean:umate": "node scripts/clean-umate-creators.js",
    "forum:backfill:professionals": "node scripts/backfill-professional-forum-threads.js"
  }
}
